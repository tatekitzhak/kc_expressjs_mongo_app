
services:
  expressjs_rest_api_app:
    container_name: nodejs_api_service # Matches central proxy hostname
    build:
      context: .
      dockerfile: Dockerfile.nodejs
    image: "expressjs_web_app_img"
    environment:
      - DATABASE_URL=mongodb://${MONGO_USERNAME}:${MONGO_PASSWORD}@db_app:27017/${DB_NAME}?authSource=admin
      # Public issuer matching client token
      - KC_ADMIN_DOMAIN_NAME=https://localhost:8443
      - KC_REALM_NAME=HTTPS_localhost_realm
      # Internal URL for container-to-container JWKS fetch over HTTP
      - KC_INTERNAL_URL=http://keycloak_container:8080
    restart: always
    extra_hosts:
      - "localhost:host-gateway"
    networks:
      - nodejs-mongodb-network
      - kc_shared_central_nginx_proxy_network # Attached to talk to the central proxy
    env_file:
      - .env
    volumes:
      - ./:/home/node/app
    depends_on:
      - db_app

  db_app:
    image: mongo:6.0.13
    container_name: mongodb_app_service
    environment:
      MONGO_INITDB_DATABASE: ${MONGO_DATABASE}
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
    networks:
      - nodejs-mongodb-network
    restart: always
    volumes:
      - mongo-data:/data/db
    ports:
      - "27017:27017" 

  mongodb_ui_app:    
    image: mongo-express
    container_name: mongodb_express_ui # Matches central proxy hostname
    restart: always
    networks:
      - nodejs-mongodb-network
      - kc_shared_central_nginx_proxy_network # Attached to talk to the central proxy
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: ${MONGO_USERNAME}
      ME_CONFIG_MONGODB_ADMINPASSWORD: ${MONGO_PASSWORD}
      ME_CONFIG_BASICAUTH_USERNAME: ${ME_USERNAME}
      ME_CONFIG_BASICAUTH_PASSWORD: ${ME_PASSWORD}
      ME_CONFIG_MONGODB_SERVER: db_app # Using service name safely
      ME_CONFIG_SITE_BASEURL: "/mongo-express/"
      ME_CONFIG_SITE_SUBPATH: "/mongo-express/"

networks:
  nodejs-mongodb-network:
    driver: bridge
  kc_shared_central_nginx_proxy_network:
    external: true # Join the shared proxy ecosystem

volumes:
  mongo-config:
  mongo-data: